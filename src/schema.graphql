type Query {
  people: [Person]
  searchPerson(name: String!, delay: Int): Person
}

type Mutation {
  addPerson(name: String): Person
}

type Subscription {
  numberIncremented: Int
}

type Person {
  id: ID
  name: String
}
